import { ContentList } from "../../components/Content";
import { SideBar } from "../../components/SideBar";

export const Main = () => {
    return (
        <>
            <SideBar />
            <ContentList />
        </>
    );
};
